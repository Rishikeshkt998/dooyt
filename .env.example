# Copy this file to .env.local and fill in real values.
# .env.local is gitignored and should NEVER be committed.

# Admin API key — used to protect /api/admin/* routes
ADMIN_API_KEY=your-secret-key-here
